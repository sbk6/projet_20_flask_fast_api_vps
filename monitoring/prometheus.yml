global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    monitor: 'shopapi-monitor'

rule_files:
  - "alerts.yml"

scrape_configs:
  - job_name: "shopapi"
    static_configs:
      - targets: ["api:8000"]
    metrics_path: "/metrics"

  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]

  - job_name: "node"
    static_configs:
      - targets: ["host.docker.internal:9100"]
